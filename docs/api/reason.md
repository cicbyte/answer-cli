# reason

## `GET` `/reasons`

**认证**: 管理员 | **参数方式**: query

**请求参数:**

  - `object_type` (`string` **(必填)**) — object_type
  - `action` (`string` **(必填)**) — action

**响应:**

- `200`:
`RespBody`

---

## `GET` `/reasons`

**认证**: 管理员 | **参数方式**: query

**请求参数:**

  - `object_type` (`string` **(必填)**) — object_type
  - `action` (`string` **(必填)**) — action

**响应:**

- `200`:
`RespBody`

---
