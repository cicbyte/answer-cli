# Permission

## `GET` `/permission`

**认证**: 管理员 | **参数方式**: query

**请求参数:**

  - `Authorization` (`string` **(必填)**) — access-token
  - `action` (`string` **(必填)**) — permission key

**响应:**

- `200`:
`RespBody`
  - `data` `object`

---
